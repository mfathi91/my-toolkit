services:
  my-toolkit:
    build: .
    container_name: my-toolkit
    ports:
      - "8765:8765"
    volumes:
      - ./temp:/app/temp
      - ./output:/app/output
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
